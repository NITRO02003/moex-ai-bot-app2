[project]
name = "app3"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["pandas", "numpy"]

[tool.pytest.ini_options]
addopts = "-q"

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
select = ["E", "F", "I"]
